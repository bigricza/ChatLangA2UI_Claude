// Test setup file for Vitest
import { afterEach, vi } from "vitest";
import { cleanup } from "@testing-library/react";
// Mock ResizeObserver which is not available in jsdom
global.ResizeObserver = class ResizeObserver {
    constructor(callback) {
        this.callback = callback;
    }
    observe() { }
    unobserve() { }
    disconnect() { }
};
// Mock scrollIntoView which is not available in jsdom
HTMLElement.prototype.scrollIntoView = vi.fn();
// Ensure we cleanup between tests to avoid lingering handles
afterEach(() => {
    cleanup();
});
// Mock @copilotkit/react-core/v2
vi.mock("@copilotkit/react-core/v2", () => ({
    useCopilotKit: vi.fn(() => ({
        copilotkit: {
            properties: {},
            setProperties: vi.fn(),
            runAgent: vi.fn().mockResolvedValue(undefined),
        },
    })),
}));
// Define custom element to avoid errors
if (!customElements.get("themed-a2ui-surface")) {
    customElements.define("themed-a2ui-surface", class extends HTMLElement {
        constructor() {
            super(...arguments);
            this.processor = null;
            this.surface = null;
            this.surfaceId = null;
            this.onAction = null;
            this.theme = null;
        }
    });
}
//# sourceMappingURL=setup.js.map